# I admit, I had no idea where to even begin here.

#First attempt:

for (var n=1)
     var output = #;
     if (var= +1)
  var output= ""
  
  #didn't work. I'm so lost on where to go from here.

size= 8

for (var n=1; n <= 100; n++) {
var output = "";
if (n % 2 == 0)
output += "#";
  
